---
phase: 06-cli-slash-command
plan: 03
type: execute
wave: 2
depends_on:
  - 06-02
files_modified:
  - .claude/commands/translate-video.md
autonomous: true
requirements:
  - CLI-02
must_haves:
  truths:
    - "Typing /translate-video in Claude Code is a recognized slash command"
    - "Claude runs yt-dubber translate $ARGUMENTS (the YouTube URL) as step 1"
    - "Claude stops and reports the error if translate exits non-zero — does NOT call dub"
    - "Claude shows the DOCX path and asks user to type 'готово' or 'done' before proceeding"
    - "Claude runs yt-dubber dub [docx_path] only after explicit user confirmation"
    - "Claude shows the MP3 path from dub output as the final step"
  artifacts:
    - path: ".claude/commands/translate-video.md"
      provides: "Claude Code slash command orchestrating the full dubbing pipeline"
      contains: "$ARGUMENTS"
  key_links:
    - from: ".claude/commands/translate-video.md"
      to: "yt-dubber translate CLI"
      via: "Bash tool invocation in Claude Code session"
      pattern: "yt-dubber translate"
    - from: ".claude/commands/translate-video.md"
      to: "yt-dubber dub CLI"
      via: "Bash tool invocation after user confirmation"
      pattern: "yt-dubber dub"
---

<objective>
Create the `/translate-video` Claude Code slash command that orchestrates the full dubbing pipeline end-to-end within a single Claude Code session.

Purpose: Users can type `/translate-video https://youtu.be/xxx` and have Claude guide them through the entire workflow — translation, DOCX review gate, TTS + assembly — without manually remembering the two-command sequence.
Output: `.claude/commands/translate-video.md` — a Markdown file with frontmatter that Claude Code serves as a slash command.
</objective>

<execution_context>
@C:/Users/abirv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/abirv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-cli-slash-command/06-CONTEXT.md

<interfaces>
<!-- Slash command format — from RESEARCH.md Pattern 7 -->

Claude Code slash command file format (.claude/commands/NAME.md):
```markdown
---
name: command-name
description: One-line description shown in Claude Code command list
argument-hint: <argument-description>
---

Instructions for Claude to follow when this command is invoked.
$ARGUMENTS is replaced with everything the user typed after /command-name.
```

Locked decisions (from CONTEXT.md — NON-NEGOTIABLE):
- File location: .claude/commands/translate-video.md
- URL input: $ARGUMENTS macro — user types /translate-video https://youtu.be/xxx
- DOCX review pause: Claude asks user to type "готово" or "done" — waits for explicit confirmation
- Error handling: If translate exits non-zero, STOP. Do NOT call dub.
- Step sequence: (1) translate, (2) show DOCX + wait for confirmation, (3) dub, (4) show MP3

Deferred (do NOT include):
- --debug flag
- Video muxing
- Any language other than JP->RU
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .claude/commands/ directory and translate-video.md</name>
  <files>.claude/commands/translate-video.md</files>
  <action>
    First, create the .claude/commands/ directory if it does not exist:
    `mkdir -p C:/Users/abirv/Desktop/CLODYA/.claude/commands`

    Then write the file .claude/commands/translate-video.md with this exact content:

    ```markdown
    ---
    name: translate-video
    description: Translate a Japanese YouTube video and dub it to Russian audio
    argument-hint: <youtube-url>
    ---

    Translate and dub the Japanese YouTube video at: $ARGUMENTS

    Follow these steps in order. Do not skip steps or reorder them.

    **Step 1 — Run the translate command:**

    Run this command using the Bash tool:
    ```
    yt-dubber translate $ARGUMENTS
    ```

    If the command exits with a non-zero exit code, stop immediately and show the full error output to the user. Do NOT proceed to Step 2.

    **Step 2 — Review gate:**

    If the translate command succeeded, extract the DOCX path from the output (it appears after "Review DOCX:").

    Tell the user exactly this:
    "Translation complete. The review DOCX is at: [docx_path from Step 1 output]

    Open that file and review the RU Translation column (column 4). You can:
    - Edit any Russian translation in column 4
    - Delete an entire row to skip that segment entirely
    - Leave rows unchanged to keep the Claude translation

    When you are done reviewing, type 'готово' or 'done' to start audio synthesis."

    Wait for the user to type 'готово' or 'done' (or a clear confirmation). Do not proceed until they confirm.

    **Step 3 — Run the dub command:**

    Once the user confirms, run this command using the Bash tool (using the DOCX path from Step 1):
    ```
    yt-dubber dub [docx_path from Step 1]
    ```

    If the dub command exits with a non-zero exit code, stop and show the error output.

    **Step 4 — Report the result:**

    If dub succeeded, extract the MP3 path from the output (it appears after "Done! MP3:") and tell the user:
    "Dubbing complete! Your Russian audio track is at: [mp3_path]"
    ```

    Important: the .claude/ directory already contains launch.json and settings.local.json — create only the commands/ subdirectory. Do NOT overwrite existing .claude/ files.
  </action>
  <verify>
    <automated>/c/Users/abirv/AppData/Local/Python/bin/python.exe -c "import pathlib; p = pathlib.Path('.claude/commands/translate-video.md'); print('EXISTS' if p.exists() else 'MISSING'); content = p.read_text(); print('HAS_ARGUMENTS' if '$ARGUMENTS' in content else 'MISSING_ARGUMENTS'); print('HAS_REVIEW_GATE' if 'done' in content.lower() else 'MISSING_REVIEW_GATE')"</automated>
  </verify>
  <done>
    - .claude/commands/translate-video.md exists with correct content
    - File contains $ARGUMENTS macro for URL passthrough
    - File contains the DOCX review gate with "готово"/"done" confirmation prompt
    - File contains error-stop logic (non-zero exit -> stop, do not call dub)
    - Step sequence: translate -> review gate -> dub -> report MP3
  </done>
</task>

</tasks>

<verification>
1. `.claude/commands/translate-video.md` exists and is non-empty
2. File contains `$ARGUMENTS` macro
3. File contains review gate instructions with "готово" or "done" confirmation
4. File contains error handling: non-zero exit from translate stops execution
5. File contains the four-step sequence: translate -> show DOCX -> wait -> dub -> show MP3
6. Existing .claude/ files (launch.json, settings.local.json) are untouched
</verification>

<success_criteria>
- .claude/commands/translate-video.md is a valid Claude Code slash command file
- $ARGUMENTS substitution passes the YouTube URL to yt-dubber translate
- DOCX review pause with explicit "готово"/"done" confirmation is present
- Error boundary: translate failure stops the workflow, dub not called
- Four-step orchestration matches the locked decisions in CONTEXT.md
</success_criteria>

<output>
After completion, create `.planning/phases/06-cli-slash-command/06-03-SUMMARY.md` following the summary template.
</output>
